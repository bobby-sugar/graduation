# 复制为 .env.development / .env.production 等（见 Vite 文档）。
# 当前业务代码仍以字面量 http://localhost:3000 为主；接入后可将图片/Socket 基址改为读取 import.meta.env.VITE_API_ORIGIN。

# 开发：与后端同源不同端口时，指向 Nest 根 URL
VITE_API_ORIGIN=http://localhost:3000

# 生产（页面与 API 同源反代时）：可留空，由代码使用 window.location.origin
# VITE_API_ORIGIN=
